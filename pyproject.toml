[project]
name = "dunc"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
dunc = "dunc.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
